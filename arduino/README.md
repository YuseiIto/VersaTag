# Arduino Sketch

Arduino Sketch.


## Requirements
- arduino-cli
- GNU Make

## Compile

```bash
make compile
```

## Upload

> [!IMPORTANT]
> Set `ARDUINO_PORT` variable in `Makefile` to your Arduino port in advance.

```bash
make upload
```
